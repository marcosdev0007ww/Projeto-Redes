server {
    listen 80;
    listen 443 ssl;
    
    # Caminho dos certificados que vamos gerar
    ssl_certificate /etc/nginx/certs/server.crt;
    ssl_certificate_key /etc/nginx/certs/server.key;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}